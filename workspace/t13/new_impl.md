# T13 New Implementation Option

## Proposed Shape

Update `README.md` so it documents the current core assessment workflow:

- Keep the concise project description.
- Replace the command list with the actual command surface.
- Add short `questions` and `score` usage examples using `questions/mbti-questions-v3.json`.
- Show the canonical answer file shape.
- Keep existing development and build instructions.

## Exclusions

- Do not add a long product manual.
- Do not duplicate every Cobra flag or every JSON result field.
- Do not promise interactive assessment, Markdown reports, profiles, or AI model calls.
- Do not create an OpenSpec change because the behavior already exists and this task only aligns docs.
