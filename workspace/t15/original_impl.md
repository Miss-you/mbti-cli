# T15 Original Implementation Notes

## Current state

- `docs/task.md` still presents all research tasks as pre-implementation work.
- `docs/plans/2026-04-11-core-assessment-design-task.md` marks T01-T14 done and shows that core assessment phase 1 has been implemented, verified, documented, and archived.
- `openspec/specs/` now contains main specs for question bank model, validator, answer parsing/validation, scoring, classifiers, renderers, `questions`, and `score`.
- README now documents the non-interactive `questions` and `score` commands and includes non-diagnostic wording.

## Drift

The human research list is no longer wrong as historical context, but it is stale as operational guidance. It implies that schema, scoring, agent input mode, output contract, fixtures, and wording still need pre-implementation decisions even though phase 1 already froze and verified those decisions.
